document.querySelector('#signin').addEventListener('submit',async event=>{event.preventDefault();const button=document.querySelector('#submit'),message=document.querySelector('#message');button.disabled=true;message.textContent='Signing in…';try{const result=await fetch('/auth/key',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({key:document.querySelector('#access-key').value.trim()})});const body=await result.json();if(!result.ok)throw Error(body.error||'Sign-in failed.');document.querySelector('#access-key').value='';location.href='/studio';}catch(error){message.textContent=error.message;message.setAttribute('role','alert');button.disabled=false;}});
